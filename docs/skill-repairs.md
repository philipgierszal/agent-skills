# Installed-skill audit repairs

Repair work follows the [2026-09-05 audit](skill-audit.md). The scope is the identified defects and instruction conflicts, not certification of every external integration. Upstream catalog entries remain pinned references; local repairs do not change those upstream revisions.

## Ownership and discovery

Keep one active owner per loader. The duplicate shared/Codex installations are disabled through supported per-path configuration, retaining their files for recovery. A maintained personal Superpowers adaptation is selected instead of simultaneously loading its divergent plugin distribution. Intentional aliases and inactive cached templates are retained.

The installed Codex loader was queried before and after configuration: enabled duplicate names fell to zero. GitHub Actions efficiency execution is disabled under the workstation's local-CI policy. These configuration changes require a fresh loader scan or client restart; an existing conversation may retain its earlier catalog. [Official skill configuration](https://learn.chatgpt.com/docs/build-skills)

## Repair coverage

| Area | Correction |
| --- | --- |
| Review scope | Distinguish committed changes from staged, unstaged and untracked work; reconcile actual scope before declaring an empty review. |
| Git safety | Stage only relevant conflict resolutions, verify the selected ignore path, preserve existing work and establish worktree creation provenance before cleanup. |
| Workflow authority | Remove false instruction hierarchy and universal activation claims; honor existing authorization and continue independent work while a real decision is pending. |
| Testing and authoring | Preserve pre-existing implementation/instructions; use meaningful behavior checks rather than deletion rituals or a test per function. Correct portable naming and description-limit guidance. |
| Supply-chain helper | Use platform ownership checks on native Windows, fail closed when ownership cannot be established, and retain regression tests. |
| LikeC4 helper | Preserve compiler failure status independently of diagnostic parsing; distinguish tool crashes from confirmed rejection of a negative example. |
| BPMN guidance | Use one coherent algorithmic layout fallback and remove unsupported transitive guarantees from direct-import searches. |
| Specification compliance | Replace the absent mandatory orchestration runtime with a usable direct review procedure and explicit coverage/unverified records. |
| Process suite | Repair contradictory decision examples, Unique-policy validation, canonical-validator composition, computed scores, approval gates and RACI semantics. Preserve existing local parser/CLI adaptations during integration. |
| Private commercial documents | Keep uncertain omissions proposed until authorized; do not treat historical leakage results as current assurance. |
| Private marketing | Require recipient-specific evidence and applicable current authority; remove mailbox/channel shortcuts, fixed deliverability guarantees and unsupported reputation isolation. |
| Specialist guidance | Clarify stack/engine boundaries, measured optimization hypotheses, dependency scope, hook preservation, host adapters and unsupported empirical claims. |

## Managed plugins

Vendor caches remain unchanged. A durable user-owned adapter, linked from global instructions, resolves local behavior for:

- Word rendering instead of the conflicting LibreOffice route.
- Sites browser QA and registered-port checks, with separate publication authority.
- Sequential Figma mutations, top-level return values, existing mapping updates and proportionate component/design scope.
- Missing plugin-discovery tools, current collaboration schemas and local-first OpenAI documentation lookup.
- Requested visual identity and language conventions instead of universal style defaults.

These are local mitigations, not claims of upstream fixes. Reassess them when the owning plugin changes. A callable integration is not evidence that credentials, account flags or a specific operation work.

## Verification and limitations

Installed and verified: **82 repaired packages, 282 individual file updates and four directory renames** across existing personal installations. Recovery copies are retained. The refreshed desktop Codex loader reports **101 enabled skills, zero duplicate names and zero loading errors**; 51 per-path exclusions and one plugin-owner selection prevent competing discovery.

| Verification | Result |
| --- | --- |
| Process suite after installation | 217 passed in Codex and 217 passed in Claude |
| Supply-chain helper after installation | 97 passed; one port-binding test intentionally excluded |
| LikeC4 error-status regression | Four offline cases passed; actual compiler acceptance not claimed |
| Personal instruction scenarios | Eight offline checks, three targeted follow-up assertions and independent Git scope/conflict cases passed |
| Public repository local gate | 35 passed, two expected Windows skips; lint, compilation and ledger validation passed |
| Public patches | Both apply to recorded baselines and reproduce the declared file hashes |

Independent specification and quality reviews passed after correcting their counterexamples, including stale approval/report binding, CLI handoff and duplicate RACI diagnostics. Instruction scenario checks are bounded evidence, not a model-wide behavioral benchmark.

The private repair record retains original snapshots, changed-file hashes, regression outputs, independent reviews and installation recovery copies. Public records exclude private client material and workstation paths. Repairs must pass their original counterexamples before installation; skipped live checks remain visible.

No campaign was sent, production service changed, Figma file mutated or document rendering result certified by this repair task. Those operations require their own concrete target and acceptance evidence. The [proposed development framework](development-workflow.md) remains a separate design proposal.
